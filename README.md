# MNIST Neural Network (NumPy from Scratch)

Train a simple fully‑connected neural network on the MNIST dataset using only **NumPy**—no TensorFlow or PyTorch.

## Implementation

- feed forward network
- CNN
